{
  "name": "poke-archive",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "node --check script.js && node --check admin.js && node --check archive-access.js",
    "test": "npm run test:ui && npm run test:rules",
    "test:ui": "node --test tests/ui.test.mjs",
    "test:rules": "firebase emulators:exec --project demo-poke-archive --only firestore --config firebase.test.json \"node --test tests/firestore.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.2",
    "jsdom": "30.1.1"
  }
}
